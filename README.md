# XenForo Entities Type

A typescript version for XenForo in API response.

## Installation

NPM:

```bash
npm install -D xenforo-entity-types
```

Yarn:

```bash
yarn add -D xenforo-entity-types
```
